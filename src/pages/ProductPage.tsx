import React from "react";
import ProductPage from "../components/products/ProductPage"; // ✅ Ensuring correct path

// const ProductEntry = ({ params }: { params: { slug: string; channel: string } }) => {
//     return <ProductPage slug={params.slug} channel={params.channel} />;
// };

// export default ProductEntry;
