import styled from "styled-components";

export const CategoryContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 20px;
    padding: 16px;
    max-width: 1200px;
    margin: auto;
`;
