"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/next/index.ts
var next_exports = {};
__export(next_exports, {
  createFylindeExternalAuthHandler: () => createFylindeExternalAuthHandler
});
module.exports = __toCommonJS(next_exports);

// src/next/handler.ts
var import_cookie = require("cookie");
var createFylindeExternalAuthHandler = (auth) => async (req, res) => {
  try {
    const { state, code } = req.query;
    if (!state || !code) {
      res.status(400).json({ error: "Missing state or code in query parameters." });
      return;
    }
    const response = await auth.obtainAccessToken({ state, code });
    if (!response || !response.token) {
      res.status(500).json({ error: "Failed to obtain access token." });
      return;
    }
    res.setHeader("Set-Cookie", (0, import_cookie.serialize)("token", response.token, { path: "/", httpOnly: true, secure: true, sameSite: "lax" }));
    res.redirect("/");
  } catch (error) {
    console.error("Error in external auth handler:", error);
    res.status(500).json({ error: "Internal Server Error" });
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createFylindeExternalAuthHandler
});
