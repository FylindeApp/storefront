import { StorageRepository } from '../types.js';
import { Request, Response } from 'express';

declare const getServerCookiesStorage: (req: Request, res: Response, options?: {
    secure?: boolean;
}) => StorageRepository;

export { getServerCookiesStorage };
